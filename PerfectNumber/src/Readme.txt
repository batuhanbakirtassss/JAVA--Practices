# PerfectNumber

This Java program checks whether a given number is a perfect number or not. It determines if a number is perfect by calculating the sum of its proper divisors (factors excluding the number itself), and comparing it to the number itself.