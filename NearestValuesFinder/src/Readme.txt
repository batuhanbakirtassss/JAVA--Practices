# Nearest Values Finder

This program finds the nearest values smaller and larger than a given target number in an array of integers.

## How to Use

1. Open a terminal or command prompt.

2. Navigate to the directory where the program source code is located.

3. Compile the program using the following command:

   ```shell
   javac NearestValuesFinder.java