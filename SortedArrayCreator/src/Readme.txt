Sorted Array Creator
This Java program allows you to create an array of integers and sorts it in ascending order.

Description
The SortedArrayCreator program takes user input to define the size of the array and then prompts the user to input the elements of the array. It uses the Arrays.sort() method to sort the array in ascending order and then displays the sorted array to the user.